apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: apps
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      nodePort: 30002
      port: 30002
      targetPort: 80
